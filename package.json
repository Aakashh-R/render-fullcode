{
  "name": "triconnect",
  "version": "1.0.0",
  "description": "TriConnect - Logistics & Documentation Platform",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "install-all": "npm install && npm -w backend install && npm -w frontend install",
    "dev": "npm run dev -w backend & npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "dev:frontend": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "build:backend": "echo 'Backend requires no build step'",
    "start": "npm start -w backend",
    "format": "npm run format -w backend && npm run format -w frontend",
    "format:check": "npm run format:check -w backend && npm run format:check -w frontend"
  },
  "devDependencies": {
    "prettier": "^3.0.0"
  }
}
